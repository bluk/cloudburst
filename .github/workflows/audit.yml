name: Rust Security Audit

on:
  push:
    paths:
      - '**/Cargo.toml'
      - '**/Cargo.lock'
  pull_request:

jobs:
  audit:
    uses: bluk/actions-workflows/.github/workflows/rust-audit.yml@e1224b81a3735f4e26fd9c88228a2af1403ed548